global jinja variables (standard_render is a stupid solution because I didn't know any better)
Safety
    Limit Upload frequency
    Limit rendering frequency
    Sanity check times
Notifications
    Notify shamers with unique url on start, completion
Shamer options
    opt out of emails
    confirm address
    combined profile for all shamer things
Generate unique-s3-url-generating session-viewer page
Thumbnails for sessions
Deploy config
    turn into template
    move all private info (keys, pws) into private ansible playbook
    include ansible playbook and substitute in things on deployment
